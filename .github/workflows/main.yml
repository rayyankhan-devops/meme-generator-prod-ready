name: Meme-Generator-CI

on:
    push:
        branches: [ main ]
    pull_request:
        branches: [ main ]

jobs:
    lint:
        uses: ./.github/workflows/lint.yml
    
    secret-scanning:
        uses: ./.github/workflows/secret-scan.yml

